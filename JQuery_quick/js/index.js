/* console.log("we are using jQuery")

$(document).ready(function() { // initializes when everything is ready
  // $("selector").action()
    // $("p").click(); // click on p


    $("p").click(function() {
        console.log("you clikced on p and its ID is => ", this); 
        // $("p").hide(); // hides
        $(this).hide(); // hides the element on which it was clicked
    });  // do this when clicked on p

    //element selector    
    // $("p").click(); // click on p

    // id selector
    // $("#second").click();

    // class selector
    $(".odd").click();

    // events in jquery

     
}); */

/* let url = "hamza.txt"

fetch(url).then(response=> {
    console.log("fetching the data");
    response.json();
}).then(data=>{
    console.log(data);
    console.log("promise done")
}); */

